<%# link_to 'New Blog', new_blog_path %>

<div class="col-md-8">
    <% @blogs.each do |blog| %>
         <!-- First Blog Post -->
         <h2 align=center>
           <%= link_to blog.title, blog %>
         </h2>
         <p class="lead" align=center>
         by <%= blog.user.firstname %> |
         <%= link_to 'Edit', edit_blog_path(blog) %> |
         <%= link_to 'Delete', blog, method: :delete, data: { confirm: 'Are you sure?' } %>
         </p>

         <p align=center><span class="glyphicon glyphicon-time"></span> Posted on August 28, 2013 at 10:00 PM</p>
         <p align=center><%= markdown blog.description %></p>
      <hr>
</div>
    <% end %>
